"""Optional evaluation harness -- not imported by pyjanus_guard itself.

Requires the ``eval`` extra: ``pip install -e ".[eval]"``. See DATASETS.md at
the repo root for what each dataset actually is and how it's licensed; this
package downloads from source at run time and never bundles dataset content.
"""
