"""Thin, optional integration examples -- none of these are imported by
pyjanus_guard's core package, and none of their third-party dependencies
(langchain-core, openai, anthropic) are core dependencies of pyjanus-guard.
Import the specific module you need directly, e.g.:

    from pyjanus_guard.integrations.langchain_callback import JanusCallbackHandler
"""
